# Main Pog package repository

This is the default package repository for the [Pog package manager](https://github.com/MatejKafka/Pog). Pog uses this repository by default, you do not need to manually configure it. The generated repository is accessible at <https://packages.pog.matejkafka.com/>.
